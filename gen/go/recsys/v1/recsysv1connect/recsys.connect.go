// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: recsys/v1/recsys.proto

package recsysv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/lucnguyen/local-youtube/gen/go/recsys/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// RecommendationServiceName is the fully-qualified name of the RecommendationService service.
	RecommendationServiceName = "recsys.v1.RecommendationService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// RecommendationServiceGetFeedProcedure is the fully-qualified name of the RecommendationService's
	// GetFeed RPC.
	RecommendationServiceGetFeedProcedure = "/recsys.v1.RecommendationService/GetFeed"
	// RecommendationServiceGetUpNextProcedure is the fully-qualified name of the
	// RecommendationService's GetUpNext RPC.
	RecommendationServiceGetUpNextProcedure = "/recsys.v1.RecommendationService/GetUpNext"
	// RecommendationServiceGetMissedProcedure is the fully-qualified name of the
	// RecommendationService's GetMissed RPC.
	RecommendationServiceGetMissedProcedure = "/recsys.v1.RecommendationService/GetMissed"
	// RecommendationServiceGetMostWatchedProcedure is the fully-qualified name of the
	// RecommendationService's GetMostWatched RPC.
	RecommendationServiceGetMostWatchedProcedure = "/recsys.v1.RecommendationService/GetMostWatched"
	// RecommendationServiceRecordSignalProcedure is the fully-qualified name of the
	// RecommendationService's RecordSignal RPC.
	RecommendationServiceRecordSignalProcedure = "/recsys.v1.RecommendationService/RecordSignal"
	// RecommendationServiceRecordImpressionsProcedure is the fully-qualified name of the
	// RecommendationService's RecordImpressions RPC.
	RecommendationServiceRecordImpressionsProcedure = "/recsys.v1.RecommendationService/RecordImpressions"
	// RecommendationServiceDeleteUserDataProcedure is the fully-qualified name of the
	// RecommendationService's DeleteUserData RPC.
	RecommendationServiceDeleteUserDataProcedure = "/recsys.v1.RecommendationService/DeleteUserData"
	// RecommendationServiceExplainFeedProcedure is the fully-qualified name of the
	// RecommendationService's ExplainFeed RPC.
	RecommendationServiceExplainFeedProcedure = "/recsys.v1.RecommendationService/ExplainFeed"
)

// RecommendationServiceClient is a client for the recsys.v1.RecommendationService service.
type RecommendationServiceClient interface {
	// Ranked ids for the home grid.
	GetFeed(context.Context, *connect.Request[v1.GetFeedRequest]) (*connect.Response[v1.GetFeedResponse], error)
	// Ranked ids for the watch page "Next" rail.
	GetUpNext(context.Context, *connect.Request[v1.GetUpNextRequest]) (*connect.Response[v1.GetUpNextResponse], error)
	// New uploads from followed channels that this viewer has not watched.
	//
	// Separate from GetFeed because it answers a different question. That one
	// asks "what should this household see", and its fresh-subscribed slot holds
	// a tenth of a page — enough that something new usually appears, and not a
	// list anybody can read to the end. This asks "did I miss anything", which
	// has an answer that runs out, and running out is the point of it.
	GetMissed(context.Context, *connect.Request[v1.GetMissedRequest]) (*connect.Response[v1.GetMissedResponse], error)
	// The videos this user has spent the most time on. Watch signals accumulate
	// during playback, so this measures time watched, not times opened.
	GetMostWatched(context.Context, *connect.Request[v1.GetMostWatchedRequest]) (*connect.Response[v1.GetMostWatchedResponse], error)
	// Behaviour signals, written on every meaningful interaction so the next
	// feed request already reflects them. No batch job in the loop.
	RecordSignal(context.Context, *connect.Request[v1.RecordSignalRequest]) (*connect.Response[v1.RecordSignalResponse], error)
	// Suppresses items already shown, so the grid does not repeat itself.
	RecordImpressions(context.Context, *connect.Request[v1.RecordImpressionsRequest]) (*connect.Response[v1.RecordImpressionsResponse], error)
	// Forget a member entirely: their signals and what they were shown.
	//
	// The first delete this service has ever had. Without it a removed profile
	// leaves its whole history behind — `u_luc` alone holds 63,908 signals — and
	// §3's rule that no service reads another's database means the gateway
	// cannot reach in and tidy up on its way past.
	DeleteUserData(context.Context, *connect.Request[v1.DeleteUserDataRequest]) (*connect.Response[v1.DeleteUserDataResponse], error)
	// The same ranking with its working shown, for tuning and for answering
	// "why is this video here" and "where did that one go".
	//
	// A debug surface with no UI behind it, and deliberately so. Every constant
	// in the ranker has been set by looking at a page and forming an impression,
	// and an impression cannot tell "this weight is wrong" from "this weight is
	// right and something downstream is discarding it" — which is precisely the
	// distinction that was missed when the quota shuffled each bucket uniformly
	// and made the entire score decorative.
	ExplainFeed(context.Context, *connect.Request[v1.ExplainFeedRequest]) (*connect.Response[v1.ExplainFeedResponse], error)
}

// NewRecommendationServiceClient constructs a client for the recsys.v1.RecommendationService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewRecommendationServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) RecommendationServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	recommendationServiceMethods := v1.File_recsys_v1_recsys_proto.Services().ByName("RecommendationService").Methods()
	return &recommendationServiceClient{
		getFeed: connect.NewClient[v1.GetFeedRequest, v1.GetFeedResponse](
			httpClient,
			baseURL+RecommendationServiceGetFeedProcedure,
			connect.WithSchema(recommendationServiceMethods.ByName("GetFeed")),
			connect.WithClientOptions(opts...),
		),
		getUpNext: connect.NewClient[v1.GetUpNextRequest, v1.GetUpNextResponse](
			httpClient,
			baseURL+RecommendationServiceGetUpNextProcedure,
			connect.WithSchema(recommendationServiceMethods.ByName("GetUpNext")),
			connect.WithClientOptions(opts...),
		),
		getMissed: connect.NewClient[v1.GetMissedRequest, v1.GetMissedResponse](
			httpClient,
			baseURL+RecommendationServiceGetMissedProcedure,
			connect.WithSchema(recommendationServiceMethods.ByName("GetMissed")),
			connect.WithClientOptions(opts...),
		),
		getMostWatched: connect.NewClient[v1.GetMostWatchedRequest, v1.GetMostWatchedResponse](
			httpClient,
			baseURL+RecommendationServiceGetMostWatchedProcedure,
			connect.WithSchema(recommendationServiceMethods.ByName("GetMostWatched")),
			connect.WithClientOptions(opts...),
		),
		recordSignal: connect.NewClient[v1.RecordSignalRequest, v1.RecordSignalResponse](
			httpClient,
			baseURL+RecommendationServiceRecordSignalProcedure,
			connect.WithSchema(recommendationServiceMethods.ByName("RecordSignal")),
			connect.WithClientOptions(opts...),
		),
		recordImpressions: connect.NewClient[v1.RecordImpressionsRequest, v1.RecordImpressionsResponse](
			httpClient,
			baseURL+RecommendationServiceRecordImpressionsProcedure,
			connect.WithSchema(recommendationServiceMethods.ByName("RecordImpressions")),
			connect.WithClientOptions(opts...),
		),
		deleteUserData: connect.NewClient[v1.DeleteUserDataRequest, v1.DeleteUserDataResponse](
			httpClient,
			baseURL+RecommendationServiceDeleteUserDataProcedure,
			connect.WithSchema(recommendationServiceMethods.ByName("DeleteUserData")),
			connect.WithClientOptions(opts...),
		),
		explainFeed: connect.NewClient[v1.ExplainFeedRequest, v1.ExplainFeedResponse](
			httpClient,
			baseURL+RecommendationServiceExplainFeedProcedure,
			connect.WithSchema(recommendationServiceMethods.ByName("ExplainFeed")),
			connect.WithClientOptions(opts...),
		),
	}
}

// recommendationServiceClient implements RecommendationServiceClient.
type recommendationServiceClient struct {
	getFeed           *connect.Client[v1.GetFeedRequest, v1.GetFeedResponse]
	getUpNext         *connect.Client[v1.GetUpNextRequest, v1.GetUpNextResponse]
	getMissed         *connect.Client[v1.GetMissedRequest, v1.GetMissedResponse]
	getMostWatched    *connect.Client[v1.GetMostWatchedRequest, v1.GetMostWatchedResponse]
	recordSignal      *connect.Client[v1.RecordSignalRequest, v1.RecordSignalResponse]
	recordImpressions *connect.Client[v1.RecordImpressionsRequest, v1.RecordImpressionsResponse]
	deleteUserData    *connect.Client[v1.DeleteUserDataRequest, v1.DeleteUserDataResponse]
	explainFeed       *connect.Client[v1.ExplainFeedRequest, v1.ExplainFeedResponse]
}

// GetFeed calls recsys.v1.RecommendationService.GetFeed.
func (c *recommendationServiceClient) GetFeed(ctx context.Context, req *connect.Request[v1.GetFeedRequest]) (*connect.Response[v1.GetFeedResponse], error) {
	return c.getFeed.CallUnary(ctx, req)
}

// GetUpNext calls recsys.v1.RecommendationService.GetUpNext.
func (c *recommendationServiceClient) GetUpNext(ctx context.Context, req *connect.Request[v1.GetUpNextRequest]) (*connect.Response[v1.GetUpNextResponse], error) {
	return c.getUpNext.CallUnary(ctx, req)
}

// GetMissed calls recsys.v1.RecommendationService.GetMissed.
func (c *recommendationServiceClient) GetMissed(ctx context.Context, req *connect.Request[v1.GetMissedRequest]) (*connect.Response[v1.GetMissedResponse], error) {
	return c.getMissed.CallUnary(ctx, req)
}

// GetMostWatched calls recsys.v1.RecommendationService.GetMostWatched.
func (c *recommendationServiceClient) GetMostWatched(ctx context.Context, req *connect.Request[v1.GetMostWatchedRequest]) (*connect.Response[v1.GetMostWatchedResponse], error) {
	return c.getMostWatched.CallUnary(ctx, req)
}

// RecordSignal calls recsys.v1.RecommendationService.RecordSignal.
func (c *recommendationServiceClient) RecordSignal(ctx context.Context, req *connect.Request[v1.RecordSignalRequest]) (*connect.Response[v1.RecordSignalResponse], error) {
	return c.recordSignal.CallUnary(ctx, req)
}

// RecordImpressions calls recsys.v1.RecommendationService.RecordImpressions.
func (c *recommendationServiceClient) RecordImpressions(ctx context.Context, req *connect.Request[v1.RecordImpressionsRequest]) (*connect.Response[v1.RecordImpressionsResponse], error) {
	return c.recordImpressions.CallUnary(ctx, req)
}

// DeleteUserData calls recsys.v1.RecommendationService.DeleteUserData.
func (c *recommendationServiceClient) DeleteUserData(ctx context.Context, req *connect.Request[v1.DeleteUserDataRequest]) (*connect.Response[v1.DeleteUserDataResponse], error) {
	return c.deleteUserData.CallUnary(ctx, req)
}

// ExplainFeed calls recsys.v1.RecommendationService.ExplainFeed.
func (c *recommendationServiceClient) ExplainFeed(ctx context.Context, req *connect.Request[v1.ExplainFeedRequest]) (*connect.Response[v1.ExplainFeedResponse], error) {
	return c.explainFeed.CallUnary(ctx, req)
}

// RecommendationServiceHandler is an implementation of the recsys.v1.RecommendationService service.
type RecommendationServiceHandler interface {
	// Ranked ids for the home grid.
	GetFeed(context.Context, *connect.Request[v1.GetFeedRequest]) (*connect.Response[v1.GetFeedResponse], error)
	// Ranked ids for the watch page "Next" rail.
	GetUpNext(context.Context, *connect.Request[v1.GetUpNextRequest]) (*connect.Response[v1.GetUpNextResponse], error)
	// New uploads from followed channels that this viewer has not watched.
	//
	// Separate from GetFeed because it answers a different question. That one
	// asks "what should this household see", and its fresh-subscribed slot holds
	// a tenth of a page — enough that something new usually appears, and not a
	// list anybody can read to the end. This asks "did I miss anything", which
	// has an answer that runs out, and running out is the point of it.
	GetMissed(context.Context, *connect.Request[v1.GetMissedRequest]) (*connect.Response[v1.GetMissedResponse], error)
	// The videos this user has spent the most time on. Watch signals accumulate
	// during playback, so this measures time watched, not times opened.
	GetMostWatched(context.Context, *connect.Request[v1.GetMostWatchedRequest]) (*connect.Response[v1.GetMostWatchedResponse], error)
	// Behaviour signals, written on every meaningful interaction so the next
	// feed request already reflects them. No batch job in the loop.
	RecordSignal(context.Context, *connect.Request[v1.RecordSignalRequest]) (*connect.Response[v1.RecordSignalResponse], error)
	// Suppresses items already shown, so the grid does not repeat itself.
	RecordImpressions(context.Context, *connect.Request[v1.RecordImpressionsRequest]) (*connect.Response[v1.RecordImpressionsResponse], error)
	// Forget a member entirely: their signals and what they were shown.
	//
	// The first delete this service has ever had. Without it a removed profile
	// leaves its whole history behind — `u_luc` alone holds 63,908 signals — and
	// §3's rule that no service reads another's database means the gateway
	// cannot reach in and tidy up on its way past.
	DeleteUserData(context.Context, *connect.Request[v1.DeleteUserDataRequest]) (*connect.Response[v1.DeleteUserDataResponse], error)
	// The same ranking with its working shown, for tuning and for answering
	// "why is this video here" and "where did that one go".
	//
	// A debug surface with no UI behind it, and deliberately so. Every constant
	// in the ranker has been set by looking at a page and forming an impression,
	// and an impression cannot tell "this weight is wrong" from "this weight is
	// right and something downstream is discarding it" — which is precisely the
	// distinction that was missed when the quota shuffled each bucket uniformly
	// and made the entire score decorative.
	ExplainFeed(context.Context, *connect.Request[v1.ExplainFeedRequest]) (*connect.Response[v1.ExplainFeedResponse], error)
}

// NewRecommendationServiceHandler builds an HTTP handler from the service implementation. It
// returns the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewRecommendationServiceHandler(svc RecommendationServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	recommendationServiceMethods := v1.File_recsys_v1_recsys_proto.Services().ByName("RecommendationService").Methods()
	recommendationServiceGetFeedHandler := connect.NewUnaryHandler(
		RecommendationServiceGetFeedProcedure,
		svc.GetFeed,
		connect.WithSchema(recommendationServiceMethods.ByName("GetFeed")),
		connect.WithHandlerOptions(opts...),
	)
	recommendationServiceGetUpNextHandler := connect.NewUnaryHandler(
		RecommendationServiceGetUpNextProcedure,
		svc.GetUpNext,
		connect.WithSchema(recommendationServiceMethods.ByName("GetUpNext")),
		connect.WithHandlerOptions(opts...),
	)
	recommendationServiceGetMissedHandler := connect.NewUnaryHandler(
		RecommendationServiceGetMissedProcedure,
		svc.GetMissed,
		connect.WithSchema(recommendationServiceMethods.ByName("GetMissed")),
		connect.WithHandlerOptions(opts...),
	)
	recommendationServiceGetMostWatchedHandler := connect.NewUnaryHandler(
		RecommendationServiceGetMostWatchedProcedure,
		svc.GetMostWatched,
		connect.WithSchema(recommendationServiceMethods.ByName("GetMostWatched")),
		connect.WithHandlerOptions(opts...),
	)
	recommendationServiceRecordSignalHandler := connect.NewUnaryHandler(
		RecommendationServiceRecordSignalProcedure,
		svc.RecordSignal,
		connect.WithSchema(recommendationServiceMethods.ByName("RecordSignal")),
		connect.WithHandlerOptions(opts...),
	)
	recommendationServiceRecordImpressionsHandler := connect.NewUnaryHandler(
		RecommendationServiceRecordImpressionsProcedure,
		svc.RecordImpressions,
		connect.WithSchema(recommendationServiceMethods.ByName("RecordImpressions")),
		connect.WithHandlerOptions(opts...),
	)
	recommendationServiceDeleteUserDataHandler := connect.NewUnaryHandler(
		RecommendationServiceDeleteUserDataProcedure,
		svc.DeleteUserData,
		connect.WithSchema(recommendationServiceMethods.ByName("DeleteUserData")),
		connect.WithHandlerOptions(opts...),
	)
	recommendationServiceExplainFeedHandler := connect.NewUnaryHandler(
		RecommendationServiceExplainFeedProcedure,
		svc.ExplainFeed,
		connect.WithSchema(recommendationServiceMethods.ByName("ExplainFeed")),
		connect.WithHandlerOptions(opts...),
	)
	return "/recsys.v1.RecommendationService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case RecommendationServiceGetFeedProcedure:
			recommendationServiceGetFeedHandler.ServeHTTP(w, r)
		case RecommendationServiceGetUpNextProcedure:
			recommendationServiceGetUpNextHandler.ServeHTTP(w, r)
		case RecommendationServiceGetMissedProcedure:
			recommendationServiceGetMissedHandler.ServeHTTP(w, r)
		case RecommendationServiceGetMostWatchedProcedure:
			recommendationServiceGetMostWatchedHandler.ServeHTTP(w, r)
		case RecommendationServiceRecordSignalProcedure:
			recommendationServiceRecordSignalHandler.ServeHTTP(w, r)
		case RecommendationServiceRecordImpressionsProcedure:
			recommendationServiceRecordImpressionsHandler.ServeHTTP(w, r)
		case RecommendationServiceDeleteUserDataProcedure:
			recommendationServiceDeleteUserDataHandler.ServeHTTP(w, r)
		case RecommendationServiceExplainFeedProcedure:
			recommendationServiceExplainFeedHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedRecommendationServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedRecommendationServiceHandler struct{}

func (UnimplementedRecommendationServiceHandler) GetFeed(context.Context, *connect.Request[v1.GetFeedRequest]) (*connect.Response[v1.GetFeedResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("recsys.v1.RecommendationService.GetFeed is not implemented"))
}

func (UnimplementedRecommendationServiceHandler) GetUpNext(context.Context, *connect.Request[v1.GetUpNextRequest]) (*connect.Response[v1.GetUpNextResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("recsys.v1.RecommendationService.GetUpNext is not implemented"))
}

func (UnimplementedRecommendationServiceHandler) GetMissed(context.Context, *connect.Request[v1.GetMissedRequest]) (*connect.Response[v1.GetMissedResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("recsys.v1.RecommendationService.GetMissed is not implemented"))
}

func (UnimplementedRecommendationServiceHandler) GetMostWatched(context.Context, *connect.Request[v1.GetMostWatchedRequest]) (*connect.Response[v1.GetMostWatchedResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("recsys.v1.RecommendationService.GetMostWatched is not implemented"))
}

func (UnimplementedRecommendationServiceHandler) RecordSignal(context.Context, *connect.Request[v1.RecordSignalRequest]) (*connect.Response[v1.RecordSignalResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("recsys.v1.RecommendationService.RecordSignal is not implemented"))
}

func (UnimplementedRecommendationServiceHandler) RecordImpressions(context.Context, *connect.Request[v1.RecordImpressionsRequest]) (*connect.Response[v1.RecordImpressionsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("recsys.v1.RecommendationService.RecordImpressions is not implemented"))
}

func (UnimplementedRecommendationServiceHandler) DeleteUserData(context.Context, *connect.Request[v1.DeleteUserDataRequest]) (*connect.Response[v1.DeleteUserDataResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("recsys.v1.RecommendationService.DeleteUserData is not implemented"))
}

func (UnimplementedRecommendationServiceHandler) ExplainFeed(context.Context, *connect.Request[v1.ExplainFeedRequest]) (*connect.Response[v1.ExplainFeedResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("recsys.v1.RecommendationService.ExplainFeed is not implemented"))
}
